export const reaxel_ = reaxel(() => {
	
	const {store,setState,mutate} = createReaxable({
		
	});
	
	const statics = {};
	
	return Object.assign(() => {},{
		store,
		setState,
		mutate,
		statics,
	})
})
